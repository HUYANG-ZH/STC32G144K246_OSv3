C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 1   


C251 COMPILER V5.60.0, COMPILATION OF MODULE zf_driver_iic
OBJECT MODULE PLACED IN .\out_file\zf_driver_iic.obj
COMPILER INVOKED BY: D:\C251keil\C251\BIN\C251.EXE ..\..\libraries\zf_driver\zf_driver_iic.c LARGE ROM(HUGE) WARNINGLEVE
                    -L(3) OPTIMIZE(0,SPEED) BROWSE INCDIR(..\..\libraries\zf_common;..\..\libraries\zf_components;..\..\libraries\zf_device;.
                    -.\..\libraries\zf_driver;..\user;..\code;..\L0_SYS;..\L1_BSP;..\L2_SERVICE;..\L3_APP;..\LL_Shared_Source;..\..\libraries
                    -\zf_components\stc32g_usb_lib;..\..\libraries\zf_components\seekfree_assistant) DEBUG PRINT(.\out_file\zf_driver_iic.lst
                    -) OBJECT(.\out_file\zf_driver_iic.obj) 

stmt  level    source

    1          /********************************************************************************************************
             -*************
    2          * STC32G144K Opensource Library 即（STC32G144K 开源库）是一个基于官方 SDK 接口的第三方
             -开源库
    3          * Copyright (c) 2025 SEEKFREE 逐飞科技
    4          *
    5          * 本文件是 STC32G144K 开源库的一部分
    6          *
    7          * STC32G144K 开源库 是免费软件
    8          * 您可以根据自由软件基金会发布的 GPL（GNU General Public License，即 GNU 通用公共�
             -�可证）的条款
    9          * 即 GPL 的第 3 版（即 GPL3.0）或（您选择的）任何后来的版本，重新发布和/或修
             -改它
   10          *
   11          * 本开源库的发布是希望它能发挥作用，但并未对其作任何保证
   12          * 甚至没有隐含的适销性或适合特定用途的保证
   13          * 更多细节请参见 GPL
   14          *
   15          * 您应该在收到本开源库的同时收到一份 GPL 的副本
   16          * 如果没有，请参阅 <https://www.gnu.org/licenses/>
   17          *
   18          * 额外注明：
   19          * 本开源库使用 GPL3.0 开源许可证协议，以上许可声明为译文版本
   20          * 许可声明英文版在 libraries/doc 文件夹下的 GPL3_permission_statement.txt 文件中
   21          * 许可证副本在 libraries 文件夹下，即该文件夹下的 LICENSE 文件
   22          * 欢迎各位使用并传播本程序，但修改内容时必须保留逐飞科技的版权声明（即�
             -��声明）
   23          *
   24          * 文件名称          zf_driver_iic.c
   25          * 公司名称          成都逐飞科技有限公司
   26          * 版本信息          查看 libraries/doc 文件夹内 version 文件的版本说明
   27          * 开发环境          MDK FOR C251
   28          * 适用平台          STC32G144K246
   29          * 店铺链接          https://seekfree.taobao.com/
   30          *
   31          * 修改记录
   32          * 日期              作者           备注
   33          * 2026-07-19        OpenAI         first version，支持 I2C1/I2C2、超时与总线恢复
   34          *********************************************************************************************************
             -***********/
   35          #include "zf_common_debug.h"
   36          #include "zf_driver_iic.h"
   37          
   38          #pragma warning disable = 177
   39          #pragma warning disable = 183
   40          
   41          #define IIC_CFG_ENABLE                 (0x80U)
   42          #define IIC_CFG_MASTER                 (0x40U)
   43          
   44          #define IIC_STATUS_BUSY                (0x80U)
   45          #define IIC_STATUS_IF                  (0x40U)
   46          #define IIC_STATUS_ACK_IN              (0x02U)
   47          
   48          #define IIC_CMD_STOP                   (0x06U)
   49          #define IIC_CMD_START_SEND_ACK         (0x09U)
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 2   

   50          #define IIC_CMD_SEND_ACK_COMBINED      (0x0AU)
   51          #define IIC_CMD_RECV_SEND_ACK          (0x0BU)
   52          #define IIC_CMD_RECV_SEND_NACK         (0x0CU)
   53          
   54          #define IIC_DATA_NONE                  (0U)
   55          #define IIC_DATA_8BIT                  (1U)
   56          #define IIC_DATA_16BIT                 (2U)
   57          
   58          typedef struct
   59          {
   60              uint8               initialized;
   61              volatile uint8      locked;
   62              uint8               default_addr;
   63              uint8               pin_group;
   64              gpio_pin_enum       scl_pin;
   65              gpio_pin_enum       sda_pin;
   66              uint16              divider;
   67              uint32              actual_speed;
   68              iic_status_enum     last_status;
   69          } iic_info_struct;
   70          
   71          static iic_info_struct iic_info[IIC_NUM];
   72          
   73          typedef enum
   74          {
   75              IIC_ASYNC_STATE_IDLE = 0,
   76              IIC_ASYNC_STATE_START_WRITE,
   77              IIC_ASYNC_STATE_WRITE,
   78              IIC_ASYNC_STATE_START_READ,
   79              IIC_ASYNC_STATE_READ,
   80              IIC_ASYNC_STATE_STOP_SUCCESS,
   81              IIC_ASYNC_STATE_STOP_ERROR,
   82          } iic_async_state_enum;
   83          
   84          typedef struct
   85          {
   86              volatile iic_async_state_enum state;
   87              volatile iic_status_enum      status;
   88              iic_status_enum               pending_status;
   89              uint8                         addr;
   90              const uint8                  *write_data;
   91              uint32                        write_len;
   92              uint32                        write_pos;
   93              uint8                        *read_data;
   94              uint32                        read_len;
   95              uint32                        read_pos;
   96              uint32                        last_progress_tick;
   97              uint32                        service_count;
   98              uint8                         timebase_valid;
   99          } iic_async_info_struct;
  100          
  101          static iic_async_info_struct iic_async_info[IIC_NUM];
  102          
  103          // IIC 总线在硬件初始化和恢复期间需要读取 SDA/SCL 实际电平。
  104          // 该配置属于 IIC 驱动自身，不能调用 GPIO 驱动文件内部的 static 函数。
  105          static void iic_set_digital_input(gpio_pin_enum pin, uint8 enable)
  106          {
  107   1          uint8 pin_bit = pin & 0x0F;
  108   1          uint8 mask = (uint8)(1 << pin_bit);
  109   1          uint8 volatile far *input_enable_register;
  110   1      
  111   1          if(pin < IO_P80)
  112   1          {
  113   2              input_enable_register = (uint8 volatile far *)(0x7efe30 + ((pin >> 4) - 1));
  114   2          }
  115   1          else
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 3   

  116   1          {
  117   2              input_enable_register = (uint8 volatile far *)(0x7ef9e0 + ((pin >> 4) - 9));
  118   2          }
  119   1      
  120   1          if(enable)
  121   1          {
  122   2              *input_enable_register |= mask;
  123   2          }
  124   1          else
  125   1          {
  126   2              *input_enable_register &= (uint8)(~mask);
  127   2          }
  128   1      }
  129          
  130          static uint8 iic_index_is_valid (iic_index_enum iic_n)
  131          {
  132   1          return ((IIC_1 == iic_n) || (IIC_2 == iic_n));
  133   1      }
  134          
  135          //-------------------------------------------------------------------------------------------------------
             -------------
  136          // 内部寄存器访问
  137          //-------------------------------------------------------------------------------------------------------
             -------------
  138          static uint8 iic_hw_get_cfg (iic_index_enum iic_n)
  139          {
  140   1          return (IIC_1 == iic_n) ? I2CCFG : I2C2CFG;
  141   1      }
  142          
  143          static void iic_hw_set_cfg (iic_index_enum iic_n, uint8 value)
  144          {
  145   1          if(IIC_1 == iic_n) I2CCFG = value;
  146   1          else               I2C2CFG = value;
  147   1      }
  148          
  149          static uint8 iic_hw_get_status (iic_index_enum iic_n)
  150          {
  151   1          return (IIC_1 == iic_n) ? I2CMSST : I2C2MSST;
  152   1      }
  153          
  154          static void iic_hw_clear_if (iic_index_enum iic_n)
  155          {
  156   1          if(IIC_1 == iic_n) I2CMSST &= (uint8)(~IIC_STATUS_IF);
  157   1          else               I2C2MSST &= (uint8)(~IIC_STATUS_IF);
  158   1      }
  159          
  160          static void iic_hw_set_command (iic_index_enum iic_n, uint8 command)
  161          {
  162   1          if(IIC_1 == iic_n) I2CMSCR = command;
  163   1          else               I2C2MSCR = command;
  164   1      }
  165          
  166          static void iic_hw_set_tx (iic_index_enum iic_n, uint8 value)
  167          {
  168   1          if(IIC_1 == iic_n) I2CTXD = value;
  169   1          else               I2C2TXD = value;
  170   1      }
  171          
  172          static uint8 iic_hw_get_rx (iic_index_enum iic_n)
  173          {
  174   1          return (IIC_1 == iic_n) ? I2CRXD : I2C2RXD;
  175   1      }
  176          
  177          static void iic_hw_set_aux (iic_index_enum iic_n, uint8 value)
  178          {
  179   1          if(IIC_1 == iic_n) I2CMSAUX = value;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 4   

  180   1          else               I2C2MSAUX = value;
  181   1      }
  182          
  183          static void iic_hw_set_prescaler (iic_index_enum iic_n, uint8 value)
  184          {
  185   1          if(IIC_1 == iic_n) I2CPSCR = value;
  186   1          else               I2C2PSCR = value;
  187   1      }
  188          
  189          static void iic_set_last_status (iic_index_enum iic_n, iic_status_enum status)
  190          {
  191   1          if(iic_index_is_valid(iic_n))
  192   1          {
  193   2              iic_info[iic_n].last_status = status;
  194   2          }
  195   1      }
  196          
  197          //-------------------------------------------------------------------------------------------------------
             -------------
  198          // 函数简介     检查引脚与 IIC 编号、通道是否严格匹配
  199          //-------------------------------------------------------------------------------------------------------
             -------------
  200          static uint8 iic_check_pin_pair (iic_index_enum iic_n, iic_pin_enum scl_pin, iic_pin_enum sda_pin)
  201          {
  202   1          uint16 group_code;
  203   1      
  204   1          if((0 == iic_index_is_valid(iic_n)) || (IIC_NULL_PIN == scl_pin) || (IIC_NULL_PIN == sda_pin))
  205   1          {
  206   2              return 0;
  207   2          }
  208   1      
  209   1          if((scl_pin & 0xFF00U) != (sda_pin & 0xFF00U))
  210   1          {
  211   2              return 0;
  212   2          }
  213   1      
  214   1          group_code = (uint16)(scl_pin & 0xFF00U);
  215   1      
  216   1          switch(iic_n)
  217   1          {
  218   2              case IIC_1:
  219   2              {
  220   3                  switch(group_code)
  221   3                  {
  222   4                      case 0x0000: return (((scl_pin & 0xFFU) == IO_P15) && ((sda_pin & 0xFFU) == IO_P14));
  223   4                      case 0x0100: return (((scl_pin & 0xFFU) == IO_P25) && ((sda_pin & 0xFFU) == IO_P24));
  224   4                      case 0x0200: return (((scl_pin & 0xFFU) == IO_P77) && ((sda_pin & 0xFFU) == IO_P76));
  225   4                      case 0x0300: return (((scl_pin & 0xFFU) == IO_P32) && ((sda_pin & 0xFFU) == IO_P33));
  226   4                      default:     return 0;
  227   4                  }
  228   3              }
  229   2      
  230   2              case IIC_2:
  231   2              {
  232   3                  switch(group_code)
  233   3                  {
  234   4                      case 0x1000: return (((scl_pin & 0xFFU) == IO_P27) && ((sda_pin & 0xFFU) == IO_P26));
  235   4                      case 0x1100: return (((scl_pin & 0xFFU) == IO_P37) && ((sda_pin & 0xFFU) == IO_P36));
  236   4                      case 0x1200: return (((scl_pin & 0xFFU) == IO_P17) && ((sda_pin & 0xFFU) == IO_P16));
  237   4                      case 0x1300: return (((scl_pin & 0xFFU) == IO_P61) && ((sda_pin & 0xFFU) == IO_P60));
  238   4                      default:     return 0;
  239   4                  }
  240   3              }
  241   2      
  242   2              case IIC_NUM:
  243   2              default:
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 5   

  244   2              {
  245   3                  return 0;
  246   3              }
  247   2          }
  248   1      }
  249          
  250          //-------------------------------------------------------------------------------------------------------
             -------------
  251          // 函数简介     设置复用引脚，且不破坏同一寄存器中的其他外设位
  252          //-------------------------------------------------------------------------------------------------------
             -------------
  253          static void iic_set_pin_mux (iic_index_enum iic_n, uint8 pin_group)
  254          {
  255   1          uint8 ea_backup;
  256   1      
  257   1          ea_backup = EA;
  258   1          EA = 0;
  259   1          EAXFR = 1;
  260   1      
  261   1          if(IIC_1 == iic_n)
  262   1          {
  263   2              P_SW2 = (uint8)((P_SW2 & (uint8)(~0x30U)) | ((pin_group & 0x03U) << 4));
  264   2          }
  265   1          else
  266   1          {
  267   2              P_SWX4 = (uint8)((P_SWX4 & (uint8)(~0xC0U)) | ((pin_group & 0x03U) << 6));
  268   2          }
  269   1      
  270   1          EA = ea_backup;
  271   1      }
  272          
  273          //-------------------------------------------------------------------------------------------------------
             -------------
  274          // 函数简介     计算 14 位 I2CMSSPEED，结果取“不高于请求速度”的最近值
  275          //-------------------------------------------------------------------------------------------------------
             -------------
  276          static uint16 iic_calculate_divider (uint32 speed)
  277          {
  278   1          uint32 required_divisor;
  279   1          uint32 divider;
  280   1      
  281   1          required_divisor = system_clock / speed;
  282   1          if(system_clock % speed)
  283   1          {
  284   2              required_divisor ++;
  285   2          }
  286   1      
  287   1          if(required_divisor <= 8U)
  288   1          {
  289   2              divider = 0;
  290   2          }
  291   1          else
  292   1          {
  293   2              divider = (required_divisor - 8U + 3U) / 4U;
  294   2          }
  295   1      
  296   1          if(divider > 0x3FFFU)
  297   1          {
  298   2              divider = 0x3FFFU;
  299   2          }
  300   1      
  301   1          return (uint16)divider;
  302   1      }
  303          
  304          static uint32 iic_calculate_actual_speed (uint16 divider)
  305          {
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 6   

  306   1          uint32 denominator = ((uint32)divider * 4U) + 8U;
  307   1          return system_clock / denominator;
  308   1      }
  309          
  310          static uint32 iic_calculate_min_speed (void)
  311          {
  312   1          uint32 denominator = ((uint32)0x3FFFU * 4U) + 8U;
  313   1          uint32 min_speed = system_clock / denominator;
  314   1      
  315   1          if(system_clock % denominator)
  316   1          {
  317   2              min_speed ++;
  318   2          }
  319   1          return min_speed;
  320   1      }
  321          
  322          //-------------------------------------------------------------------------------------------------------
             -------------
  323          // 函数简介     重新装载硬件寄存器。关闭自动发送和中断，采用轮询状态机。
  324          //-------------------------------------------------------------------------------------------------------
             -------------
  325          static void iic_hw_configure (iic_index_enum iic_n)
  326          {
  327   1          uint8 cfg;
  328   1      
  329   1          iic_hw_set_cfg(iic_n, 0x00);
  330   1          iic_hw_set_aux(iic_n, 0x00);
  331   1          iic_hw_set_command(iic_n, 0x00);
  332   1          iic_hw_clear_if(iic_n);
  333   1          iic_hw_set_prescaler(iic_n, (uint8)(iic_info[iic_n].divider >> 6));
  334   1      
  335   1          cfg = (uint8)(IIC_CFG_MASTER | (iic_info[iic_n].divider & 0x3FU));
  336   1          iic_hw_set_cfg(iic_n, cfg);
  337   1          iic_hw_set_cfg(iic_n, (uint8)(cfg | IIC_CFG_ENABLE));
  338   1      }
  339          
  340          //-------------------------------------------------------------------------------------------------------
             -------------
  341          // 函数简介     尝试获得非重入锁。只在检查并置位的极短区间关闭全局中断。
  342          //-------------------------------------------------------------------------------------------------------
             -------------
  343          static uint8 iic_try_lock (iic_index_enum iic_n)
  344          {
  345   1          uint8 ea_backup;
  346   1          uint8 result = 0;
  347   1      
  348   1          ea_backup = EA;
  349   1          EA = 0;
  350   1          if(0 == iic_info[iic_n].locked)
  351   1          {
  352   2              iic_info[iic_n].locked = 1;
  353   2              result = 1;
  354   2          }
  355   1          EA = ea_backup;
  356   1      
  357   1          return result;
  358   1      }
  359          
  360          static void iic_unlock (iic_index_enum iic_n)
  361          {
  362   1          uint8 ea_backup;
  363   1      
  364   1          ea_backup = EA;
  365   1          EA = 0;
  366   1          iic_info[iic_n].locked = 0;
  367   1          EA = ea_backup;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 7   

  368   1      }
  369          
  370          //-------------------------------------------------------------------------------------------------------
             -------------
  371          // 非阻塞硬件 IIC 状态机。每一次 iic_async_process 只观察一次状态位或下发一条硬
             -件命令，
  372          // 从不在主循环、定时器或 DMA 中断里等待 SCL/SDA/IF。
  373          //-------------------------------------------------------------------------------------------------------
             -------------
  374          static void iic_async_issue_command (iic_index_enum iic_n, uint8 command)
  375          {
  376   1          iic_hw_clear_if(iic_n);
  377   1          iic_hw_set_command(iic_n, command);
  378   1      }
  379          
  380          static void iic_async_finish (iic_index_enum iic_n, iic_status_enum status)
  381          {
  382   1          iic_async_info[iic_n].state = IIC_ASYNC_STATE_IDLE;
  383   1          iic_async_info[iic_n].status = status;
  384   1          iic_async_info[iic_n].timebase_valid = 0U;
  385   1          iic_set_last_status(iic_n, status);
  386   1          iic_unlock(iic_n);
  387   1      }
  388          
  389          static void iic_async_begin_stop (iic_index_enum iic_n, iic_status_enum final_status)
  390          {
  391   1          iic_async_info[iic_n].pending_status = final_status;
  392   1          iic_async_info[iic_n].state = (IIC_SUCCESS == final_status) ?
  393   1                  IIC_ASYNC_STATE_STOP_SUCCESS : IIC_ASYNC_STATE_STOP_ERROR;
  394   1          iic_async_issue_command(iic_n, IIC_CMD_STOP);
  395   1      }
  396          
  397          //-------------------------------------------------------------------------------------------------------
             -------------
  398          // 函数简介     提交一个 7-bit IIC 异步读写事务。提交后由 iic_async_process 推进，�
             -��用点不阻塞。
  399          //-------------------------------------------------------------------------------------------------------
             -------------
  400          iic_status_enum iic_async_transfer (iic_index_enum iic_n, uint8 addr,
  401                  const uint8 *write_data, uint32 write_len, uint8 *read_data, uint32 read_len)
  402          {
  403   1          iic_async_info_struct *async_info;
  404   1      
  405   1          if((0 == iic_index_is_valid(iic_n)) || (addr > 0x7FU) ||
  406   1             ((write_len > 0U) && (NULL == write_data)) ||
  407   1             ((read_len > 0U) && (NULL == read_data)))
  408   1          {
  409   2              iic_set_last_status(iic_n, IIC_ERROR_INVALID_PARAMETER);
  410   2              return IIC_ERROR_INVALID_PARAMETER;
  411   2          }
  412   1      
  413   1          if(0U == iic_info[iic_n].initialized)
  414   1          {
  415   2              iic_set_last_status(iic_n, IIC_ERROR_NOT_INITIALIZED);
  416   2              return IIC_ERROR_NOT_INITIALIZED;
  417   2          }
  418   1      
  419   1          if((0U == write_len) && (0U == read_len))
  420   1          {
  421   2              iic_set_last_status(iic_n, IIC_SUCCESS);
  422   2              return IIC_SUCCESS;
  423   2          }
  424   1      
  425   1          if(0U == iic_try_lock(iic_n))
  426   1          {
  427   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 8   

  428   2              return IIC_ERROR_BUSY;
  429   2          }
  430   1      
  431   1          EAXFR = 1;
  432   1          if(0U != (iic_hw_get_status(iic_n) & IIC_STATUS_BUSY))
  433   1          {
  434   2              iic_unlock(iic_n);
  435   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
  436   2              return IIC_ERROR_BUSY;
  437   2          }
  438   1      
  439   1          if((0U == gpio_get_level(iic_info[iic_n].scl_pin)) ||
  440   1             (0U == gpio_get_level(iic_info[iic_n].sda_pin)))
  441   1          {
  442   2              iic_unlock(iic_n);
  443   2              iic_set_last_status(iic_n, IIC_ERROR_BUS_STUCK);
  444   2              return IIC_ERROR_BUS_STUCK;
  445   2          }
  446   1      
  447   1          async_info = &iic_async_info[iic_n];
  448   1          async_info->addr = addr;
  449   1          async_info->write_data = write_data;
  450   1          async_info->write_len = write_len;
  451   1          async_info->write_pos = 0U;
  452   1          async_info->read_data = read_data;
  453   1          async_info->read_len = read_len;
  454   1          async_info->read_pos = 0U;
  455   1          async_info->pending_status = IIC_SUCCESS;
  456   1          async_info->last_progress_tick = 0U;
  457   1          async_info->service_count = 0U;
  458   1          async_info->timebase_valid = 0U;
  459   1          async_info->status = IIC_ERROR_BUSY;
  460   1          iic_set_last_status(iic_n, IIC_ERROR_BUSY);
  461   1      
  462   1          if(write_len > 0U)
  463   1          {
  464   2              iic_hw_set_tx(iic_n, (uint8)((addr << 1) | 0U));
  465   2              async_info->state = IIC_ASYNC_STATE_START_WRITE;
  466   2          }
  467   1          else
  468   1          {
  469   2              iic_hw_set_tx(iic_n, (uint8)((addr << 1) | 1U));
  470   2              async_info->state = IIC_ASYNC_STATE_START_READ;
  471   2          }
  472   1          iic_async_issue_command(iic_n, IIC_CMD_START_SEND_ACK);
  473   1          return IIC_SUCCESS;
  474   1      }
  475          
  476          //-------------------------------------------------------------------------------------------------------
             -------------
  477          // 函数简介     推进一步异步硬件 IIC 事务。必须仅在主循环或专用低优先级服�
             -�上下文调用。
  478          //-------------------------------------------------------------------------------------------------------
             -------------
  479          static void iic_async_process_internal (iic_index_enum iic_n, uint8 timed, uint32 now_tick)
  480          {
  481   1          iic_async_info_struct *async_info;
  482   1          uint8 hw_status;
  483   1      
  484   1          if((0 == iic_index_is_valid(iic_n)) ||
  485   1             (IIC_ASYNC_STATE_IDLE == iic_async_info[iic_n].state))
  486   1          {
  487   2              return;
  488   2          }
  489   1      
  490   1          EAXFR = 1;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 9   

  491   1          async_info = &iic_async_info[iic_n];
  492   1          hw_status = iic_hw_get_status(iic_n);
  493   1          if(0U == (hw_status & IIC_STATUS_IF))
  494   1          {
  495   2              async_info->service_count++;
  496   2              if((0U != timed) && (0U == async_info->timebase_valid))
  497   2              {
  498   3                  async_info->last_progress_tick = now_tick;
  499   3                  async_info->timebase_valid = 1U;
  500   3              }
  501   2              if(((0U != timed) && ((uint32)(now_tick - async_info->last_progress_tick) >= IIC_ASYNC_TIMEOUT_TI
             -CKS)) ||
  502   2                 ((0U == timed) && (async_info->service_count >= IIC_ASYNC_MAX_SERVICE_COUNT)))
  503   2              {
  504   3                  /* 不执行 GPIO 位带恢复，避免把等待循环带入常规业务路径。 */
  505   3                  iic_hw_configure(iic_n);
  506   3                  iic_async_finish(iic_n, IIC_ERROR_TIMEOUT);
  507   3              }
  508   2              return;
  509   2          }
  510   1      
  511   1          async_info->service_count = 0U;
  512   1          if(0U != timed)
  513   1          {
  514   2              async_info->last_progress_tick = now_tick;
  515   2              async_info->timebase_valid = 1U;
  516   2          }
  517   1          iic_hw_clear_if(iic_n);
  518   1      
  519   1          switch(async_info->state)
  520   1          {
  521   2              case IIC_ASYNC_STATE_START_WRITE:
  522   2                  if(0U != (hw_status & IIC_STATUS_ACK_IN))
  523   2                  {
  524   3                      iic_async_begin_stop(iic_n, IIC_ERROR_NACK);
  525   3                  }
  526   2                  else
  527   2                  {
  528   3                      iic_hw_set_tx(iic_n, async_info->write_data[async_info->write_pos++]);
  529   3                      async_info->state = IIC_ASYNC_STATE_WRITE;
  530   3                      iic_async_issue_command(iic_n, IIC_CMD_SEND_ACK_COMBINED);
  531   3                  }
  532   2                  break;
  533   2      
  534   2              case IIC_ASYNC_STATE_WRITE:
  535   2                  if(0U != (hw_status & IIC_STATUS_ACK_IN))
  536   2                  {
  537   3                      iic_async_begin_stop(iic_n, IIC_ERROR_NACK);
  538   3                  }
  539   2                  else if(async_info->write_pos < async_info->write_len)
  540   2                  {
  541   3                      iic_hw_set_tx(iic_n, async_info->write_data[async_info->write_pos++]);
  542   3                      iic_async_issue_command(iic_n, IIC_CMD_SEND_ACK_COMBINED);
  543   3                  }
  544   2                  else if(async_info->read_len > 0U)
  545   2                  {
  546   3                      iic_hw_set_tx(iic_n, (uint8)((async_info->addr << 1) | 1U));
  547   3                      async_info->state = IIC_ASYNC_STATE_START_READ;
  548   3                      iic_async_issue_command(iic_n, IIC_CMD_START_SEND_ACK);
  549   3                  }
  550   2                  else
  551   2                  {
  552   3                      iic_async_begin_stop(iic_n, IIC_SUCCESS);
  553   3                  }
  554   2                  break;
  555   2      
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 10  

  556   2              case IIC_ASYNC_STATE_START_READ:
  557   2                  if(0U != (hw_status & IIC_STATUS_ACK_IN))
  558   2                  {
  559   3                      iic_async_begin_stop(iic_n, IIC_ERROR_NACK);
  560   3                  }
  561   2                  else
  562   2                  {
  563   3                      async_info->state = IIC_ASYNC_STATE_READ;
  564   3                      iic_async_issue_command(iic_n,
  565   3                              (1U == async_info->read_len) ? IIC_CMD_RECV_SEND_NACK : IIC_CMD_RECV_SEND_ACK);
  566   3                  }
  567   2                  break;
  568   2      
  569   2              case IIC_ASYNC_STATE_READ:
  570   2                  async_info->read_data[async_info->read_pos++] = iic_hw_get_rx(iic_n);
  571   2                  if(async_info->read_pos < async_info->read_len)
  572   2                  {
  573   3                      iic_async_issue_command(iic_n,
  574   3                              ((async_info->read_pos + 1U) == async_info->read_len) ?
  575   3                              IIC_CMD_RECV_SEND_NACK : IIC_CMD_RECV_SEND_ACK);
  576   3                  }
  577   2                  else
  578   2                  {
  579   3                      iic_async_begin_stop(iic_n, IIC_SUCCESS);
  580   3                  }
  581   2                  break;
  582   2      
  583   2              case IIC_ASYNC_STATE_STOP_SUCCESS:
  584   2              case IIC_ASYNC_STATE_STOP_ERROR:
  585   2                  iic_async_finish(iic_n, async_info->pending_status);
  586   2                  break;
  587   2      
  588   2              case IIC_ASYNC_STATE_IDLE:
  589   2              default:
  590   2                  iic_async_finish(iic_n, IIC_ERROR_TIMEOUT);
  591   2                  break;
  592   2          }
  593   1      }
  594          
  595          void iic_async_process (iic_index_enum iic_n)
  596          {
  597   1          iic_async_process_internal(iic_n, 0U, 0UL);
  598   1      }
  599          
  600          void iic_async_process_all (void)
  601          {
  602   1          iic_async_process(IIC_1);
  603   1          iic_async_process(IIC_2);
  604   1      }
  605          
  606          void iic_async_process_timed (iic_index_enum iic_n, uint32 now_tick)
  607          {
  608   1          iic_async_process_internal(iic_n, 1U, now_tick);
  609   1      }
  610          
  611          void iic_async_process_all_timed (uint32 now_tick)
  612          {
  613   1          iic_async_process_timed(IIC_1, now_tick);
  614   1          iic_async_process_timed(IIC_2, now_tick);
  615   1      }
  616          
  617          uint8 iic_async_is_busy (iic_index_enum iic_n)
  618          {
  619   1          return (iic_index_is_valid(iic_n) &&
  620   1                  (IIC_ASYNC_STATE_IDLE != iic_async_info[iic_n].state)) ? 1U : 0U;
  621   1      }
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 11  

  622          
  623          iic_status_enum iic_async_get_status (iic_index_enum iic_n)
  624          {
  625   1          if(0 == iic_index_is_valid(iic_n))
  626   1          {
  627   2              return IIC_ERROR_INVALID_PARAMETER;
  628   2          }
  629   1      
  630   1          return (0U != iic_async_is_busy(iic_n)) ? IIC_ERROR_BUSY : iic_async_info[iic_n].status;
  631   1      }
  632          
  633          //-------------------------------------------------------------------------------------------------------
             -------------
  634          // 函数简介     总线恢复专用短延时。使用 CPU 空循环，不占用工程中的 Timer11 �
             -�其他硬件定时器。
  635          // 备注信息     总线恢复只要求脉冲不短于目标值，不参与正常 IIC 位时序；实�
             -�延时允许略长。
  636          //-------------------------------------------------------------------------------------------------------
             -------------
  637          static void iic_recovery_delay_us (uint16 period_us)
  638          {
  639   1          volatile uint32 delay_count;
  640   1          uint32 loops_per_us;
  641   1      
  642   1          loops_per_us = system_clock / 1000000UL;
  643   1          if(0U == loops_per_us)
  644   1          {
  645   2              loops_per_us = 1U;
  646   2          }
  647   1      
  648   1          if(0U == period_us)
  649   1          {
  650   2              period_us = 1U;
  651   2          }
  652   1      
  653   1          delay_count = loops_per_us * (uint32)period_us;
  654   1          while(delay_count)
  655   1          {
  656   2              delay_count --;
  657   2              _nop_();
  658   2          }
  659   1      }
  660          
  661          //-------------------------------------------------------------------------------------------------------
             -------------
  662          // 函数简介     执行一个硬件命令并等待完成；任何路径都有限时，不存在死循�
             -��。
  663          //-------------------------------------------------------------------------------------------------------
             -------------
  664          static iic_status_enum iic_execute_command (iic_index_enum iic_n, uint8 command)
  665          {
  666   1          uint32 timeout = IIC_TIMEOUT_COUNT;
  667   1      
  668   1          iic_hw_clear_if(iic_n);
  669   1          iic_hw_set_command(iic_n, command);
  670   1      
  671   1          while(0 == (iic_hw_get_status(iic_n) & IIC_STATUS_IF))
  672   1          {
  673   2              if(0 == timeout --)
  674   2              {
  675   3                  return IIC_ERROR_TIMEOUT;
  676   3              }
  677   2          }
  678   1      
  679   1          iic_hw_clear_if(iic_n);
  680   1          return IIC_SUCCESS;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 12  

  681   1      }
  682          
  683          //-------------------------------------------------------------------------------------------------------
             -------------
  684          // 函数简介     GPIO 层总线恢复：最多 9 个 SCL 脉冲并生成 STOP。
  685          //-------------------------------------------------------------------------------------------------------
             -------------
  686          static iic_status_enum iic_recover_bus_internal (iic_index_enum iic_n)
  687          {
  688   1          uint8 pulse_count;
  689   1          uint16 wait_count;
  690   1          gpio_pin_enum scl_pin = iic_info[iic_n].scl_pin;
  691   1          gpio_pin_enum sda_pin = iic_info[iic_n].sda_pin;
  692   1      
  693   1          iic_hw_set_cfg(iic_n, (uint8)(iic_hw_get_cfg(iic_n) & (uint8)(~IIC_CFG_ENABLE)));
  694   1      
  695   1          gpio_init(scl_pin, GPO, GPIO_HIGH, GPO_OPEN_DTAIN);
  696   1          gpio_init(sda_pin, GPO, GPIO_HIGH, GPO_OPEN_DTAIN);
  697   1          iic_set_digital_input(scl_pin, GPIO_DIGITAL_IN_ENABLE);
  698   1          iic_set_digital_input(sda_pin, GPIO_DIGITAL_IN_ENABLE);
  699   1          iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  700   1      
  701   1          // SCL 被目标器件永久拉低时，主机无法恢复，只能明确返回错误。
  702   1          wait_count = 1000U;
  703   1          while((0 == gpio_get_level(scl_pin)) && wait_count --)
  704   1          {
  705   2              iic_recovery_delay_us(1);
  706   2          }
  707   1          if(0 == gpio_get_level(scl_pin))
  708   1          {
  709   2              iic_hw_configure(iic_n);
  710   2              return IIC_ERROR_BUS_STUCK;
  711   2          }
  712   1      
  713   1          for(pulse_count = 0; (pulse_count < 9U) && (0 == gpio_get_level(sda_pin)); pulse_count ++)
  714   1          {
  715   2              gpio_low(scl_pin);
  716   2              iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  717   2              gpio_high(scl_pin);
  718   2      
  719   2              wait_count = 1000U;
  720   2              while((0 == gpio_get_level(scl_pin)) && wait_count --)
  721   2              {
  722   3                  iic_recovery_delay_us(1);
  723   3              }
  724   2              if(0 == gpio_get_level(scl_pin))
  725   2              {
  726   3                  iic_hw_configure(iic_n);
  727   3                  return IIC_ERROR_BUS_STUCK;
  728   3              }
  729   2              iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  730   2          }
  731   1      
  732   1          // 生成一个标准 STOP：SDA 低 -> SCL 高 -> SDA 高。
  733   1          gpio_low(sda_pin);
  734   1          iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  735   1          gpio_high(scl_pin);
  736   1          iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  737   1          gpio_high(sda_pin);
  738   1          iic_recovery_delay_us(IIC_RECOVERY_PULSE_US);
  739   1      
  740   1          iic_set_pin_mux(iic_n, iic_info[iic_n].pin_group);
  741   1          iic_hw_configure(iic_n);
  742   1      
  743   1          if((0 == gpio_get_level(scl_pin)) || (0 == gpio_get_level(sda_pin)))
  744   1          {
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 13  

  745   2              return IIC_ERROR_BUS_STUCK;
  746   2          }
  747   1      
  748   1          return IIC_SUCCESS;
  749   1      }
  750          
  751          //-------------------------------------------------------------------------------------------------------
             -------------
  752          // 函数简介     等待总线空闲，超时后进行一次恢复。
  753          //-------------------------------------------------------------------------------------------------------
             -------------
  754          static iic_status_enum iic_prepare_transaction (iic_index_enum iic_n)
  755          {
  756   1          uint32 timeout = IIC_TIMEOUT_COUNT;
  757   1      
  758   1          // I2C1/I2C2 寄存器位于 XFR；防止用户代码曾关闭 EAXFR 后访问失效。
  759   1          EAXFR = 1;
  760   1      
  761   1          while(iic_hw_get_status(iic_n) & IIC_STATUS_BUSY)
  762   1          {
  763   2              if(0 == timeout --)
  764   2              {
  765   3                  return iic_recover_bus_internal(iic_n);
  766   3              }
  767   2          }
  768   1      
  769   1          if((0 == gpio_get_level(iic_info[iic_n].scl_pin)) ||
  770   1             (0 == gpio_get_level(iic_info[iic_n].sda_pin)))
  771   1          {
  772   2              return iic_recover_bus_internal(iic_n);
  773   2          }
  774   1      
  775   1          return IIC_SUCCESS;
  776   1      }
  777          
  778          //-------------------------------------------------------------------------------------------------------
             -------------
  779          // 函数简介     发送 START/Repeated START、地址和读写位，并检查目标应答。
  780          //-------------------------------------------------------------------------------------------------------
             -------------
  781          static iic_status_enum iic_start_address (iic_index_enum iic_n, uint8 addr, uint8 read)
  782          {
  783   1          iic_status_enum status;
  784   1      
  785   1          iic_hw_set_tx(iic_n, (uint8)(((uint32)addr << 1) | (read ? 1U : 0U)));
  786   1          status = iic_execute_command(iic_n, IIC_CMD_START_SEND_ACK);
  787   1          if(IIC_SUCCESS != status)
  788   1          {
  789   2              return status;
  790   2          }
  791   1      
  792   1          return (iic_hw_get_status(iic_n) & IIC_STATUS_ACK_IN) ? IIC_ERROR_NACK : IIC_SUCCESS;
  793   1      }
  794          
  795          //-------------------------------------------------------------------------------------------------------
             -------------
  796          // 函数简介     发送一个字节并检查 ACK。
  797          //-------------------------------------------------------------------------------------------------------
             -------------
  798          static iic_status_enum iic_send_byte (iic_index_enum iic_n, uint8 value)
  799          {
  800   1          iic_status_enum status;
  801   1      
  802   1          iic_hw_set_tx(iic_n, value);
  803   1          status = iic_execute_command(iic_n, IIC_CMD_SEND_ACK_COMBINED);
  804   1          if(IIC_SUCCESS != status)
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 14  

  805   1          {
  806   2              return status;
  807   2          }
  808   1      
  809   1          return (iic_hw_get_status(iic_n) & IIC_STATUS_ACK_IN) ? IIC_ERROR_NACK : IIC_SUCCESS;
  810   1      }
  811          
  812          //-------------------------------------------------------------------------------------------------------
             -------------
  813          // 函数简介     接收一个字节。最后一字节发送 NACK，其余发送 ACK。
  814          //-------------------------------------------------------------------------------------------------------
             -------------
  815          static iic_status_enum iic_receive_byte (iic_index_enum iic_n, uint8 *value, uint8 last_byte)
  816          {
  817   1          iic_status_enum status;
  818   1      
  819   1          status = iic_execute_command(iic_n, last_byte ? IIC_CMD_RECV_SEND_NACK : IIC_CMD_RECV_SEND_ACK);
  820   1          if(IIC_SUCCESS == status)
  821   1          {
  822   2              *value = iic_hw_get_rx(iic_n);
  823   2          }
  824   1          return status;
  825   1      }
  826          
  827          //-------------------------------------------------------------------------------------------------------
             -------------
  828          // 函数简介     尽最大努力发送 STOP。失败时复位模块并恢复总线，防止后续调�
             -�永久卡死。
  829          //-------------------------------------------------------------------------------------------------------
             -------------
  830          static iic_status_enum iic_stop_transaction (iic_index_enum iic_n)
  831          {
  832   1          iic_status_enum status;
  833   1      
  834   1          status = iic_execute_command(iic_n, IIC_CMD_STOP);
  835   1          if(IIC_SUCCESS != status)
  836   1          {
  837   2              iic_recover_bus_internal(iic_n);
  838   2              return status;
  839   2          }
  840   1      
  841   1          return IIC_SUCCESS;
  842   1      }
  843          
  844          //-------------------------------------------------------------------------------------------------------
             -------------
  845          // 函数简介     统一事务引擎。支持 0/1/2 字节前缀、8/16 位数据、Repeated START 与 
             -SCCB STOP。
  846          //-------------------------------------------------------------------------------------------------------
             -------------
  847          static iic_status_enum iic_transaction_engine (
  848              iic_index_enum iic_n,
  849              uint8 addr,
  850              const uint8 *prefix,
  851              uint8 prefix_len,
  852              const void *write_data,
  853              uint32 write_len,
  854              uint8 write_width,
  855              void *read_data,
  856              uint32 read_len,
  857              uint8 read_width,
  858              uint8 stop_between)
  859          {
  860   1          iic_status_enum status = IIC_SUCCESS;
  861   1          iic_status_enum stop_status;
  862   1          uint8 started = 0;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 15  

  863   1          uint8 value;
  864   1          uint8 index;
  865   1          uint32 count;
  866   1          const uint8 *write8;
  867   1          const uint16 *write16;
  868   1          uint8 *read8;
  869   1          uint16 *read16;
  870   1      
  871   1          if((0 == iic_index_is_valid(iic_n)) || (addr > 0x7FU) || (prefix_len > 2U) ||
  872   1             ((write_width != IIC_DATA_NONE) && (write_width != IIC_DATA_8BIT) && (write_width != IIC_DATA_16BI
             -T)) ||
  873   1             ((read_width != IIC_DATA_NONE) && (read_width != IIC_DATA_8BIT) && (read_width != IIC_DATA_16BIT))
             - ||
  874   1             ((prefix_len > 0U) && (NULL == prefix)) ||
  875   1             ((write_len > 0U) && ((NULL == write_data) || (IIC_DATA_NONE == write_width))) ||
  876   1             ((read_len > 0U) && ((NULL == read_data) || (IIC_DATA_NONE == read_width))))
  877   1          {
  878   2              iic_set_last_status(iic_n, IIC_ERROR_INVALID_PARAMETER);
  879   2              return IIC_ERROR_INVALID_PARAMETER;
  880   2          }
  881   1      
  882   1          if(0 == iic_info[iic_n].initialized)
  883   1          {
  884   2              iic_set_last_status(iic_n, IIC_ERROR_NOT_INITIALIZED);
  885   2              return IIC_ERROR_NOT_INITIALIZED;
  886   2          }
  887   1      
  888   1          if((0U == prefix_len) && (0U == write_len) && (0U == read_len))
  889   1          {
  890   2              iic_set_last_status(iic_n, IIC_SUCCESS);
  891   2              return IIC_SUCCESS;
  892   2          }
  893   1      
  894   1          if(0 == iic_try_lock(iic_n))
  895   1          {
  896   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
  897   2              return IIC_ERROR_BUSY;
  898   2          }
  899   1      
  900   1          status = iic_prepare_transaction(iic_n);
  901   1          if(IIC_SUCCESS != status)
  902   1          {
  903   2              goto transaction_exit;
  904   2          }
  905   1      
  906   1          // 有前缀或写数据时，先开启写方向事务。
  907   1          if((prefix_len > 0U) || (write_len > 0U))
  908   1          {
  909   2              status = iic_start_address(iic_n, addr, 0);
  910   2              started = 1;
  911   2              if(IIC_SUCCESS != status) goto transaction_fail;
  912   2      
  913   2              for(index = 0; index < prefix_len; index ++)
  914   2              {
  915   3                  status = iic_send_byte(iic_n, prefix[index]);
  916   3                  if(IIC_SUCCESS != status) goto transaction_fail;
  917   3              }
  918   2      
  919   2              if(IIC_DATA_8BIT == write_width)
  920   2              {
  921   3                  write8 = (const uint8 *)write_data;
  922   3                  for(count = 0; count < write_len; count ++)
  923   3                  {
  924   4                      status = iic_send_byte(iic_n, write8[count]);
  925   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  926   4                  }
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 16  

  927   3              }
  928   2              else if(IIC_DATA_16BIT == write_width)
  929   2              {
  930   3                  write16 = (const uint16 *)write_data;
  931   3                  for(count = 0; count < write_len; count ++)
  932   3                  {
  933   4                      status = iic_send_byte(iic_n, (uint8)(write16[count] >> 8));
  934   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  935   4                      status = iic_send_byte(iic_n, (uint8)write16[count]);
  936   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  937   4                  }
  938   3              }
  939   2          }
  940   1      
  941   1          if(read_len > 0U)
  942   1          {
  943   2              if(stop_between && started)
  944   2              {
  945   3                  status = iic_stop_transaction(iic_n);
  946   3                  if(IIC_SUCCESS != status) goto transaction_exit;
  947   3      
  948   3                  status = iic_prepare_transaction(iic_n);
  949   3                  if(IIC_SUCCESS != status) goto transaction_exit;
  950   3              }
  951   2      
  952   2              status = iic_start_address(iic_n, addr, 1);
  953   2              started = 1;
  954   2              if(IIC_SUCCESS != status) goto transaction_fail;
  955   2      
  956   2              if(IIC_DATA_8BIT == read_width)
  957   2              {
  958   3                  read8 = (uint8 *)read_data;
  959   3                  for(count = 0; count < read_len; count ++)
  960   3                  {
  961   4                      status = iic_receive_byte(iic_n, &read8[count], (uint8)((count + 1U) == read_len));
  962   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  963   4                  }
  964   3              }
  965   2              else if(IIC_DATA_16BIT == read_width)
  966   2              {
  967   3                  read16 = (uint16 *)read_data;
  968   3                  for(count = 0; count < read_len; count ++)
  969   3                  {
  970   4                      status = iic_receive_byte(iic_n, &value, 0);
  971   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  972   4                      read16[count] = (uint16)((uint16)value << 8);
  973   4      
  974   4                      status = iic_receive_byte(iic_n, &value, (uint8)((count + 1U) == read_len));
  975   4                      if(IIC_SUCCESS != status) goto transaction_fail;
  976   4                      read16[count] |= value;
  977   4                  }
  978   3              }
  979   2          }
  980   1      
  981   1          if(started)
  982   1          {
  983   2              status = iic_stop_transaction(iic_n);
  984   2          }
  985   1          goto transaction_exit;
  986   1      
  987   1      transaction_fail:
  988   1          if(started)
  989   1          {
  990   2              if(IIC_ERROR_TIMEOUT == status)
  991   2              {
  992   3                  // 硬件命令已卡住时不再追加 STOP 命令，直接关闭模块并恢复总线。
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 17  

  993   3                  iic_recover_bus_internal(iic_n);
  994   3              }
  995   2              else
  996   2              {
  997   3                  stop_status = iic_stop_transaction(iic_n);
  998   3                  if(IIC_SUCCESS != stop_status)
  999   3                  {
 1000   4                      status = stop_status;
 1001   4                  }
 1002   3              }
 1003   2          }
 1004   1      
 1005   1      transaction_exit:
 1006   1          iic_set_last_status(iic_n, status);
 1007   1          iic_unlock(iic_n);
 1008   1          return status;
 1009   1      }
 1010          
 1011          //-------------------------------------------------------------------------------------------------------
             -------------
 1012          // 函数简介     初始化硬件 IIC，默认主机、7 位地址、轮询模式
 1013          //-------------------------------------------------------------------------------------------------------
             -------------
 1014          iic_status_enum iic_init (iic_index_enum iic_n, uint8 addr, uint32 speed, iic_pin_enum scl_pin, iic_pin_e
             -num sda_pin)
 1015          {
 1016   1          uint16 divider;
 1017   1          uint8 pin_group;
 1018   1      
 1019   1          if((0 == iic_index_is_valid(iic_n)) || (addr > 0x7FU) || (0U == speed) ||
 1020   1             (0U == system_clock) ||
 1021   1             (speed < iic_calculate_min_speed()) ||
 1022   1             (0 == iic_check_pin_pair(iic_n, scl_pin, sda_pin)))
 1023   1          {
 1024   2              iic_set_last_status(iic_n, IIC_ERROR_INVALID_PARAMETER);
 1025   2              return IIC_ERROR_INVALID_PARAMETER;
 1026   2          }
 1027   1      
 1028   1          if(0 == iic_try_lock(iic_n))
 1029   1          {
 1030   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
 1031   2              return IIC_ERROR_BUSY;
 1032   2          }
 1033   1      
 1034   1          divider = iic_calculate_divider(speed);
 1035   1          pin_group = (uint8)((scl_pin >> 8) & 0x03U);
 1036   1      
 1037   1          iic_info[iic_n].initialized  = 0;
 1038   1          iic_info[iic_n].default_addr = addr;
 1039   1          iic_info[iic_n].pin_group    = pin_group;
 1040   1          iic_info[iic_n].scl_pin      = (gpio_pin_enum)(scl_pin & 0xFFU);
 1041   1          iic_info[iic_n].sda_pin      = (gpio_pin_enum)(sda_pin & 0xFFU);
 1042   1          iic_info[iic_n].divider      = divider;
 1043   1          iic_info[iic_n].actual_speed = iic_calculate_actual_speed(divider);
 1044   1          iic_info[iic_n].last_status  = IIC_SUCCESS;
 1045   1      
 1046   1          gpio_init(iic_info[iic_n].scl_pin, GPO, GPIO_HIGH, GPO_OPEN_DTAIN);
 1047   1          gpio_init(iic_info[iic_n].sda_pin, GPO, GPIO_HIGH, GPO_OPEN_DTAIN);
 1048   1          iic_set_digital_input(iic_info[iic_n].scl_pin, GPIO_DIGITAL_IN_ENABLE);
 1049   1          iic_set_digital_input(iic_info[iic_n].sda_pin, GPIO_DIGITAL_IN_ENABLE);
 1050   1          iic_set_pin_mux(iic_n, pin_group);
 1051   1          iic_hw_configure(iic_n);
 1052   1          iic_info[iic_n].initialized = 1;
 1053   1      
 1054   1          /* Runtime initialization must be bounded.  Do not enter the legacy
 1055   1             GPIO-pulse recovery loop here: the active ToF path reports BUS_STUCK
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 18  

 1056   1             and retries through its background state machine instead. */
 1057   1          if((0 == gpio_get_level(iic_info[iic_n].scl_pin)) ||
 1058   1             (0 == gpio_get_level(iic_info[iic_n].sda_pin)))
 1059   1          {
 1060   2              iic_info[iic_n].last_status = IIC_ERROR_BUS_STUCK;
 1061   2          }
 1062   1      
 1063   1          iic_unlock(iic_n);
 1064   1          return iic_info[iic_n].last_status;
 1065   1      }
 1066          
 1067          //-------------------------------------------------------------------------------------------------------
             -------------
 1068          // 带状态返回的底层接口
 1069          //-------------------------------------------------------------------------------------------------------
             -------------
 1070          iic_status_enum iic_write (iic_index_enum iic_n, uint8 addr, const uint8 *buffer, uint32 len)
 1071          {
 1072   1          return iic_transaction_engine(iic_n, addr, NULL, 0, buffer, len, IIC_DATA_8BIT, NULL, 0, IIC_DATA_NON
             -E, 0);
 1073   1      }
 1074          
 1075          iic_status_enum iic_read (iic_index_enum iic_n, uint8 addr, uint8 *buffer, uint32 len)
 1076          {
 1077   1          return iic_transaction_engine(iic_n, addr, NULL, 0, NULL, 0, IIC_DATA_NONE, buffer, len, IIC_DATA_8BI
             -T, 0);
 1078   1      }
 1079          
 1080          iic_status_enum iic_transfer (iic_index_enum iic_n, uint8 addr, const uint8 *write_data, uint32 write_len
             -, uint8 *read_data, uint32 read_len)
 1081          {
 1082   1          return iic_transaction_engine(iic_n, addr, NULL, 0, write_data, write_len, IIC_DATA_8BIT, read_data, 
             -read_len, IIC_DATA_8BIT, 0);
 1083   1      }
 1084          
 1085          iic_status_enum iic_recover_bus (iic_index_enum iic_n)
 1086          {
 1087   1          iic_status_enum status;
 1088   1      
 1089   1          if(0 == iic_index_is_valid(iic_n)) return IIC_ERROR_INVALID_PARAMETER;
 1090   1          if(0 == iic_info[iic_n].initialized)
 1091   1          {
 1092   2              iic_set_last_status(iic_n, IIC_ERROR_NOT_INITIALIZED);
 1093   2              return IIC_ERROR_NOT_INITIALIZED;
 1094   2          }
 1095   1          if(0 == iic_try_lock(iic_n))
 1096   1          {
 1097   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
 1098   2              return IIC_ERROR_BUSY;
 1099   2          }
 1100   1      
 1101   1          status = iic_recover_bus_internal(iic_n);
 1102   1          iic_set_last_status(iic_n, status);
 1103   1          iic_unlock(iic_n);
 1104   1          return status;
 1105   1      }
 1106          
 1107          iic_status_enum iic_get_last_status (iic_index_enum iic_n)
 1108          {
 1109   1          return iic_index_is_valid(iic_n) ? iic_info[iic_n].last_status : IIC_ERROR_INVALID_PARAMETER;
 1110   1      }
 1111          
 1112          uint32 iic_get_actual_speed (iic_index_enum iic_n)
 1113          {
 1114   1          return (iic_index_is_valid(iic_n) && iic_info[iic_n].initialized) ? iic_info[iic_n].actual_speed : 0U
             -;
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 19  

 1115   1      }
 1116          
 1117          void iic_deinit (iic_index_enum iic_n)
 1118          {
 1119   1          if(0 == iic_index_is_valid(iic_n))
 1120   1          {
 1121   2              return;
 1122   2          }
 1123   1      
 1124   1          if(0 == iic_try_lock(iic_n))
 1125   1          {
 1126   2              iic_set_last_status(iic_n, IIC_ERROR_BUSY);
 1127   2              return;
 1128   2          }
 1129   1      
 1130   1          EAXFR = 1;
 1131   1          iic_hw_set_cfg(iic_n, 0x00);
 1132   1          iic_info[iic_n].initialized = 0;
 1133   1          iic_info[iic_n].last_status = IIC_ERROR_NOT_INITIALIZED;
 1134   1          iic_unlock(iic_n);
 1135   1      }
 1136          
 1137          //-------------------------------------------------------------------------------------------------------
             -------------
 1138          // 与软件 IIC 风格一致的兼容封装
 1139          //-------------------------------------------------------------------------------------------------------
             -------------
 1140          void iic_write_8bit (iic_index_enum iic_n, uint8 addr, const uint8 dat)
 1141          {
 1142   1          iic_write(iic_n, addr, &dat, 1);
 1143   1      }
 1144          
 1145          void iic_write_8bit_array (iic_index_enum iic_n, uint8 addr, const uint8 *dat, uint32 len)
 1146          {
 1147   1          iic_write(iic_n, addr, dat, len);
 1148   1      }
 1149          
 1150          void iic_write_16bit (iic_index_enum iic_n, uint8 addr, const uint16 dat)
 1151          {
 1152   1          iic_transaction_engine(iic_n, addr, NULL, 0, &dat, 1, IIC_DATA_16BIT, NULL, 0, IIC_DATA_NONE, 0);
 1153   1      }
 1154          
 1155          void iic_write_16bit_array (iic_index_enum iic_n, uint8 addr, const uint16 *dat, uint32 len)
 1156          {
 1157   1          iic_transaction_engine(iic_n, addr, NULL, 0, dat, len, IIC_DATA_16BIT, NULL, 0, IIC_DATA_NONE, 0);
 1158   1      }
 1159          
 1160          void iic_write_8bit_register (iic_index_enum iic_n, uint8 addr, const uint8 register_name, const uint8 da
             -t)
 1161          {
 1162   1          iic_transaction_engine(iic_n, addr, &register_name, 1, &dat, 1, IIC_DATA_8BIT, NULL, 0, IIC_DATA_NONE
             -, 0);
 1163   1      }
 1164          
 1165          void iic_write_8bit_registers (iic_index_enum iic_n, uint8 addr, const uint8 register_name, const uint8 *
             -dat, uint32 len)
 1166          {
 1167   1          iic_transaction_engine(iic_n, addr, &register_name, 1, dat, len, IIC_DATA_8BIT, NULL, 0, IIC_DATA_NON
             -E, 0);
 1168   1      }
 1169          
 1170          void iic_write_16bit_register (iic_index_enum iic_n, uint8 addr, const uint16 register_name, const uint16
             - dat)
 1171          {
 1172   1          uint8 prefix[2];
 1173   1          prefix[0] = (uint8)(register_name >> 8);
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 20  

 1174   1          prefix[1] = (uint8)register_name;
 1175   1          iic_transaction_engine(iic_n, addr, prefix, 2, &dat, 1, IIC_DATA_16BIT, NULL, 0, IIC_DATA_NONE, 0);
 1176   1      }
 1177          
 1178          void iic_write_16bit_registers (iic_index_enum iic_n, uint8 addr, const uint16 register_name, const uint1
             -6 *dat, uint32 len)
 1179          {
 1180   1          uint8 prefix[2];
 1181   1          prefix[0] = (uint8)(register_name >> 8);
 1182   1          prefix[1] = (uint8)register_name;
 1183   1          iic_transaction_engine(iic_n, addr, prefix, 2, dat, len, IIC_DATA_16BIT, NULL, 0, IIC_DATA_NONE, 0);
 1184   1      }
 1185          
 1186          uint8 iic_read_8bit (iic_index_enum iic_n, uint8 addr)
 1187          {
 1188   1          uint8 dat = 0;
 1189   1          iic_read(iic_n, addr, &dat, 1);
 1190   1          return dat;
 1191   1      }
 1192          
 1193          void iic_read_8bit_array (iic_index_enum iic_n, uint8 addr, uint8 *dat, uint32 len)
 1194          {
 1195   1          iic_read(iic_n, addr, dat, len);
 1196   1      }
 1197          
 1198          uint16 iic_read_16bit (iic_index_enum iic_n, uint8 addr)
 1199          {
 1200   1          uint16 dat = 0;
 1201   1          iic_transaction_engine(iic_n, addr, NULL, 0, NULL, 0, IIC_DATA_NONE, &dat, 1, IIC_DATA_16BIT, 0);
 1202   1          return dat;
 1203   1      }
 1204          
 1205          void iic_read_16bit_array (iic_index_enum iic_n, uint8 addr, uint16 *dat, uint32 len)
 1206          {
 1207   1          iic_transaction_engine(iic_n, addr, NULL, 0, NULL, 0, IIC_DATA_NONE, dat, len, IIC_DATA_16BIT, 0);
 1208   1      }
 1209          
 1210          uint8 iic_read_8bit_register (iic_index_enum iic_n, uint8 addr, const uint8 register_name)
 1211          {
 1212   1          uint8 dat = 0;
 1213   1          iic_transaction_engine(iic_n, addr, &register_name, 1, NULL, 0, IIC_DATA_NONE, &dat, 1, IIC_DATA_8BIT
             -, 0);
 1214   1          return dat;
 1215   1      }
 1216          
 1217          void iic_read_8bit_registers (iic_index_enum iic_n, uint8 addr, const uint8 register_name, uint8 *dat, ui
             -nt32 len)
 1218          {
 1219   1          iic_transaction_engine(iic_n, addr, &register_name, 1, NULL, 0, IIC_DATA_NONE, dat, len, IIC_DATA_8BI
             -T, 0);
 1220   1      }
 1221          
 1222          uint16 iic_read_16bit_register (iic_index_enum iic_n, uint8 addr, const uint16 register_name)
 1223          {
 1224   1          uint8 prefix[2];
 1225   1          uint16 dat = 0;
 1226   1          prefix[0] = (uint8)(register_name >> 8);
 1227   1          prefix[1] = (uint8)register_name;
 1228   1          iic_transaction_engine(iic_n, addr, prefix, 2, NULL, 0, IIC_DATA_NONE, &dat, 1, IIC_DATA_16BIT, 0);
 1229   1          return dat;
 1230   1      }
 1231          
 1232          void iic_read_16bit_registers (iic_index_enum iic_n, uint8 addr, const uint16 register_name, uint16 *dat,
             - uint32 len)
 1233          {
 1234   1          uint8 prefix[2];
C251 COMPILER V5.60.0,  zf_driver_iic                                                      29/07/26  19:15:03  PAGE 21  

 1235   1          prefix[0] = (uint8)(register_name >> 8);
 1236   1          prefix[1] = (uint8)register_name;
 1237   1          iic_transaction_engine(iic_n, addr, prefix, 2, NULL, 0, IIC_DATA_NONE, dat, len, IIC_DATA_16BIT, 0);
 1238   1      }
 1239          
 1240          void iic_transfer_8bit_array (iic_index_enum iic_n, uint8 addr, const uint8 *write_data, uint32 write_len
             -, uint8 *read_data, uint32 read_len)
 1241          {
 1242   1          iic_transfer(iic_n, addr, write_data, write_len, read_data, read_len);
 1243   1      }
 1244          
 1245          void iic_transfer_16bit_array (iic_index_enum iic_n, uint8 addr, const uint16 *write_data, uint32 write_l
             -en, uint16 *read_data, uint32 read_len)
 1246          {
 1247   1          iic_transaction_engine(iic_n, addr, NULL, 0, write_data, write_len, IIC_DATA_16BIT, read_data, read_l
             -en, IIC_DATA_16BIT, 0);
 1248   1      }
 1249          
 1250          void iic_sccb_write_register (iic_index_enum iic_n, uint8 addr, const uint8 register_name, uint8 dat)
 1251          {
 1252   1          iic_write_8bit_register(iic_n, addr, register_name, dat);
 1253   1      }
 1254          
 1255          uint8 iic_sccb_read_register (iic_index_enum iic_n, uint8 addr, const uint8 register_name)
 1256          {
 1257   1          uint8 dat = 0;
 1258   1          iic_transaction_engine(iic_n, addr, &register_name, 1, NULL, 0, IIC_DATA_NONE, &dat, 1, IIC_DATA_8BIT
             -, 1);
 1259   1          return dat;
 1260   1      }


Module Information          Static   Overlayable
------------------------------------------------
  code size            =    ------     ------
  ecode size           =     11693     ------
  data size            =    ------     ------
  idata size           =    ------     ------
  pdata size           =    ------     ------
  xdata size           =       627     ------
  xdata-const size     =    ------     ------
  edata size           =    ------     ------
  bit size             =    ------     ------
  ebit size            =    ------     ------
  bitaddressable size  =    ------     ------
  ebitaddressable size =    ------     ------
  far data size        =    ------     ------
  huge data size       =    ------     ------
  const size           =    ------     ------
  hconst size          =    ------     ------
End of Module Information.


C251 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
