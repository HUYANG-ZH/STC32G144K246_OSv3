C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 1   


C251 COMPILER V5.60.0, COMPILATION OF MODULE service_packet
OBJECT MODULE PLACED IN .\out_file\service_packet.obj
COMPILER INVOKED BY: D:\C251keil\C251\BIN\C251.EXE ..\L2_SERVICE\service_packet.c LARGE ROM(HUGE) WARNINGLEVEL(3) OPTIMI
                    -ZE(0,SPEED) BROWSE INCDIR(..\..\libraries\zf_common;..\..\libraries\zf_components;..\..\libraries\zf_device;..\..\librar
                    -ies\zf_driver;..\user;..\code;..\L0_SYS;..\L1_BSP;..\L2_SERVICE;..\L3_APP;..\LL_Shared_Source;..\..\libraries\zf_compone
                    -nts\stc32g_usb_lib;..\..\libraries\zf_components\seekfree_assistant) DEBUG PRINT(.\out_file\service_packet.lst) OBJECT(.
                    -\out_file\service_packet.obj) 

stmt  level    source

    1          #include "zf_common_headfile.h"
    2          #include "service_function_queue.h"
    3          #include "service_wireless_uart.h"
    4          #include "service_packet.h"
    5          
    6          #define SERVICE_PACKET_RX_READ_SIZE       (64U)
    7          #define SERVICE_PACKET_FRAME_MAX          (96U)
    8          #define SERVICE_PACKET_TX_FRAME_MAX       (128U)
    9          #define SERVICE_PACKET_NAME_SNAPSHOT_MAX  (32U)
   10          #define SERVICE_PACKET_TRIGGER_PRIORITY   (1U)
   11          #define SERVICE_PACKET_REPLY_FLOAT_SCALE  (1000.0f)
   12          #define SERVICE_PACKET_REPLY_FLOAT_BASE   (1000UL)
   13          
   14          typedef struct
   15          {
   16              const char *name;
   17              float *value;
   18              uint8 value_count;
   19              service_packet_write_callback_t callback;
   20          } service_packet_variable_t;
   21          
   22          typedef struct
   23          {
   24              const char *name;
   25              service_packet_action_func_t func;
   26              uint32 delay_ms;
   27          } service_packet_action_t;
   28          
   29          typedef enum
   30          {
   31              SERVICE_PACKET_RX_WAIT_HEAD_1 = 0,
   32              SERVICE_PACKET_RX_WAIT_HEAD_2,
   33              SERVICE_PACKET_RX_BODY,
   34              SERVICE_PACKET_RX_TAIL_2,
   35          } service_packet_rx_state_t;
   36          
   37          static service_packet_variable_t packet_variables[SERVICE_PACKET_VARIABLE_MAX];
   38          static service_packet_action_t packet_actions[SERVICE_PACKET_ACTION_MAX];
   39          static uint8 packet_variable_count = 0U;
   40          static uint8 packet_action_count = 0U;
   41          
   42          static service_packet_rx_state_t packet_rx_state = SERVICE_PACKET_RX_WAIT_HEAD_1;
   43          static char packet_frame[SERVICE_PACKET_FRAME_MAX];
   44          static char packet_reply[SERVICE_PACKET_TX_FRAME_MAX];
   45          static uint8 packet_frame_index = 0U;
   46          static volatile uint32 packet_rx_byte_total = 0U;
   47          static volatile uint32 packet_frame_total = 0U;
   48          static volatile uint32 packet_read_total = 0U;
   49          static volatile uint32 packet_read_miss_total = 0U;
   50          static volatile uint32 packet_write_total = 0U;
   51          static volatile uint32 packet_write_ok_total = 0U;
   52          static volatile uint32 packet_write_miss_total = 0U;
   53          static volatile uint32 packet_write_parse_fail_total = 0U;
   54          static char packet_last_miss_name[SERVICE_PACKET_NAME_SNAPSHOT_MAX];
   55          
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 2   

   56          static void service_packet_save_miss_name(const char *name);
   57          static void service_packet_append_char(char *buffer, uint8 *index, char value);
   58          static void service_packet_append_string(char *buffer, uint8 *index, const char *text);
   59          static void service_packet_append_uint32(char *buffer, uint8 *index, uint32 value);
   60          static void service_packet_append_float3(char *buffer, uint8 *index, float value);
   61          static uint8 service_packet_value_changed(service_packet_variable_t *variable, const float *value);
   62          
   63          //-------------------------------------------------------------------------------------------------------
             -------------
   64          // 函数简介     判断字符是否为帧字段分隔符
   65          // 参数说明     value           待判断字符
   66          // 返回参数     uint8           1：是字段分隔符 0：不是字段分隔符
   67          // 使用示例     内部调用，用户无需关心
   68          //-------------------------------------------------------------------------------------------------------
             -------------
   69          static uint8 service_packet_is_field_separator(char value)
   70          {
   71   1          return ((',' == value) || (':' == value) || ('=' == value) ||
   72   1                  (' ' == value) || ('\t' == value));
   73   1      }
   74          
   75          //-------------------------------------------------------------------------------------------------------
             -------------
   76          // 函数简介     判断字符是否为数值列表分隔符
   77          // 参数说明     value           待判断字符
   78          // 返回参数     uint8           1：是数值分隔符 0：不是数值分隔符
   79          // 使用示例     内部调用，用户无需关心
   80          //-------------------------------------------------------------------------------------------------------
             -------------
   81          static uint8 service_packet_is_value_separator(char value)
   82          {
   83   1          return ((',' == value) || (';' == value) || ('|' == value) ||
   84   1                  (' ' == value) || ('\t' == value));
   85   1      }
   86          
   87          //-------------------------------------------------------------------------------------------------------
             -------------
   88          // 函数简介     跳过字段分隔符
   89          // 参数说明     text            字符串当前位置
   90          // 返回参数     char *          跳过分隔符后的字符串位置
   91          // 使用示例     内部调用，用户无需关心
   92          //-------------------------------------------------------------------------------------------------------
             -------------
   93          static char *service_packet_skip_field_separator(char *text)
   94          {
   95   1          while(('\0' != *text) && (0U != service_packet_is_field_separator(*text)))
   96   1          {
   97   2              text++;
   98   2          }
   99   1      
  100   1          return text;
  101   1      }
  102          
  103          //-------------------------------------------------------------------------------------------------------
             -------------
  104          // 函数简介     跳过数值列表分隔符
  105          // 参数说明     text            字符串当前位置
  106          // 返回参数     char *          跳过分隔符后的字符串位置
  107          // 使用示例     内部调用，用户无需关心
  108          //-------------------------------------------------------------------------------------------------------
             -------------
  109          static char *service_packet_skip_value_separator(char *text)
  110          {
  111   1          while(('\0' != *text) && (0U != service_packet_is_value_separator(*text)))
  112   1          {
  113   2              text++;
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 3   

  114   2          }
  115   1      
  116   1          return text;
  117   1      }
  118          
  119          //-------------------------------------------------------------------------------------------------------
             -------------
  120          // 函数简介     判断字符是否为浮点数字符
  121          // 参数说明     value           待判断字符
  122          // 返回参数     uint8           1：是浮点数字符 0：不是浮点数字符
  123          // 使用示例     内部调用，用户无需关心
  124          //-------------------------------------------------------------------------------------------------------
             -------------
  125          static uint8 service_packet_is_number_char(char value)
  126          {
  127   1          return ((('0' <= value) && ('9' >= value)) || ('+' == value) ||
  128   1                  ('-' == value) || ('.' == value));
  129   1      }
  130          
  131          //-------------------------------------------------------------------------------------------------------
             -------------
  132          // 函数简介     根据变量名查找变量映射
  133          // 参数说明     name            变量名称
  134          // 返回参数     service_packet_variable_t * 变量映射指针 NULL：未找到
  135          // 使用示例     内部调用，用户无需关心
  136          //-------------------------------------------------------------------------------------------------------
             -------------
  137          static service_packet_variable_t *service_packet_find_variable(const char *name)
  138          {
  139   1          uint8 i;
  140   1      
  141   1          for(i = 0; i < packet_variable_count; i++)
  142   1          {
  143   2              if(0 == strcmp(packet_variables[i].name, name))
  144   2              {
  145   3                  return &packet_variables[i];
  146   3              }
  147   2          }
  148   1      
  149   1          return NULL;
  150   1      }
  151          
  152          //-------------------------------------------------------------------------------------------------------
             -------------
  153          // 函数简介     根据操作名查找触发动作映射
  154          // 参数说明     name            操作名称
  155          // 返回参数     service_packet_action_t *   动作映射指针 NULL：未找到
  156          // 使用示例     内部调用，用户无需关心
  157          //-------------------------------------------------------------------------------------------------------
             -------------
  158          static service_packet_action_t *service_packet_find_action(const char *name)
  159          {
  160   1          uint8 i;
  161   1      
  162   1          for(i = 0; i < packet_action_count; i++)
  163   1          {
  164   2              if(0 == strcmp(packet_actions[i].name, name))
  165   2              {
  166   3                  return &packet_actions[i];
  167   3              }
  168   2          }
  169   1      
  170   1          return NULL;
  171   1      }
  172          
  173          static void service_packet_save_miss_name(const char *name)
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 4   

  174          {
  175   1          uint8 i = 0U;
  176   1      
  177   1          if(NULL == name)
  178   1          {
  179   2              packet_last_miss_name[0] = '\0';
  180   2              return;
  181   2          }
  182   1      
  183   1          while(('\0' != name[i]) && (i < (SERVICE_PACKET_NAME_SNAPSHOT_MAX - 1U)))
  184   1          {
  185   2              packet_last_miss_name[i] = name[i];
  186   2              i++;
  187   2          }
  188   1      
  189   1          packet_last_miss_name[i] = '\0';
  190   1      }
  191          
  192          static void service_packet_append_char(char *buffer, uint8 *index, char value)
  193          {
  194   1          if(*index < (SERVICE_PACKET_TX_FRAME_MAX - 1U))
  195   1          {
  196   2              buffer[*index] = value;
  197   2              (*index)++;
  198   2          }
  199   1      }
  200          
  201          static void service_packet_append_string(char *buffer, uint8 *index, const char *text)
  202          {
  203   1          if(NULL == text)
  204   1          {
  205   2              return;
  206   2          }
  207   1      
  208   1          while('\0' != *text)
  209   1          {
  210   2              service_packet_append_char(buffer, index, *text);
  211   2              text++;
  212   2          }
  213   1      }
  214          
  215          static void service_packet_append_uint32(char *buffer, uint8 *index, uint32 value)
  216          {
  217   1          char temp[10];
  218   1          uint8 temp_index = 0U;
  219   1      
  220   1          if(0UL == value)
  221   1          {
  222   2              service_packet_append_char(buffer, index, '0');
  223   2              return;
  224   2          }
  225   1      
  226   1          while((0UL != value) && (temp_index < sizeof(temp)))
  227   1          {
  228   2              temp[temp_index] = (char)('0' + (value % 10UL));
  229   2              value /= 10UL;
  230   2              temp_index++;
  231   2          }
  232   1      
  233   1          while(0U != temp_index)
  234   1          {
  235   2              temp_index--;
  236   2              service_packet_append_char(buffer, index, temp[temp_index]);
  237   2          }
  238   1      }
  239          
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 5   

  240          static void service_packet_append_float3(char *buffer, uint8 *index, float value)
  241          {
  242   1          uint32 scaled;
  243   1          uint32 integer_part;
  244   1          uint32 fraction_part;
  245   1      
  246   1          if(0.0f > value)
  247   1          {
  248   2              service_packet_append_char(buffer, index, '-');
  249   2              value = 0.0f - value;
  250   2          }
  251   1      
  252   1          scaled = (uint32)((value * SERVICE_PACKET_REPLY_FLOAT_SCALE) + 0.5f);
  253   1          integer_part = scaled / SERVICE_PACKET_REPLY_FLOAT_BASE;
  254   1          fraction_part = scaled % SERVICE_PACKET_REPLY_FLOAT_BASE;
  255   1      
  256   1          service_packet_append_uint32(buffer, index, integer_part);
  257   1          service_packet_append_char(buffer, index, '.');
  258   1          service_packet_append_char(buffer, index, (char)('0' + (fraction_part / 100UL)));
  259   1          fraction_part %= 100UL;
  260   1          service_packet_append_char(buffer, index, (char)('0' + (fraction_part / 10UL)));
  261   1          service_packet_append_char(buffer, index, (char)('0' + (fraction_part % 10UL)));
  262   1      }
  263          
  264          static uint8 service_packet_value_changed(service_packet_variable_t *variable, const float *value)
  265          {
  266   1          uint8 i;
  267   1      
  268   1          if((NULL == variable) || (NULL == value))
  269   1          {
  270   2              return 0U;
  271   2          }
  272   1      
  273   1          for(i = 0U; i < variable->value_count; i++)
  274   1          {
  275   2              if(variable->value[i] != value[i])
  276   2              {
  277   3                  return 1U;
  278   3              }
  279   2          }
  280   1      
  281   1          return 0U;
  282   1      }
  283          
  284          //-------------------------------------------------------------------------------------------------------
             -------------
  285          // 函数简介     解析浮点数列表
  286          // 参数说明     text            数值字符串
  287          // 参数说明     out_value       解析结果保存地址
  288          // 参数说明     value_count     需要解析的数值个数
  289          // 返回参数     uint8           1：解析成功 0：解析失败
  290          // 使用示例     内部调用，用户无需关心
  291          //-------------------------------------------------------------------------------------------------------
             -------------
  292          static uint8 service_packet_parse_float_list(char *text, float *out_value, uint8 value_count)
  293          {
  294   1          uint8 i;
  295   1          uint8 digit_found;
  296   1          uint8 point_found;
  297   1          char *token_start;
  298   1          char delimiter;
  299   1      
  300   1          for(i = 0; i < value_count; i++)
  301   1          {
  302   2              text = service_packet_skip_value_separator(text);
  303   2              if('\0' == *text)
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 6   

  304   2              {
  305   3                  return 0U;
  306   3              }
  307   2      
  308   2              token_start = text;
  309   2              digit_found = 0U;
  310   2              point_found = 0U;
  311   2      
  312   2              if(('+' == *text) || ('-' == *text))
  313   2              {
  314   3                  text++;
  315   3              }
  316   2      
  317   2              while(0U != service_packet_is_number_char(*text))
  318   2              {
  319   3                  if(('.' == *text) && (0U != point_found))
  320   3                  {
  321   4                      return 0U;
  322   4                  }
  323   3                  else if('.' == *text)
  324   3                  {
  325   4                      point_found = 1U;
  326   4                  }
  327   3                  else if(('0' <= *text) && ('9' >= *text))
  328   3                  {
  329   4                      digit_found = 1U;
  330   4                  }
  331   3      
  332   3                  text++;
  333   3              }
  334   2      
  335   2              if(0U == digit_found)
  336   2              {
  337   3                  return 0U;
  338   3              }
  339   2      
  340   2              if(('\0' != *text) && (0U == service_packet_is_value_separator(*text)))
  341   2              {
  342   3                  return 0U;
  343   3              }
  344   2      
  345   2              delimiter = *text;
  346   2              *text = '\0';
  347   2              out_value[i] = func_str_to_float(token_start);
  348   2      
  349   2              if('\0' == delimiter)
  350   2              {
  351   3                  return (i == (value_count - 1U));
  352   3              }
  353   2      
  354   2              text++;
  355   2          }
  356   1      
  357   1          text = service_packet_skip_value_separator(text);
  358   1      
  359   1          return ('\0' == *text);
  360   1      }
  361          
  362          //-------------------------------------------------------------------------------------------------------
             -------------
  363          // 函数简介     通过无线串口输出变量名称和当前数值
  364          // 参数说明     variable        变量映射指针
  365          // 返回参数     void
  366          // 使用示例     内部调用，用户无需关心
  367          // 备注信息     输出格式为 name,value0,value1...\r\n
  368          //-------------------------------------------------------------------------------------------------------
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 7   

             -------------
  369          static void service_packet_print_variable(service_packet_variable_t *variable)
  370          {
  371   1          uint8 i;
  372   1          uint8 index = 0U;
  373   1          uint8 ea_backup;
  374   1          float value_copy[SERVICE_PACKET_VALUE_MAX];
  375   1      
  376   1          ea_backup = EA;
  377   1          EA = 0;
  378   1          for(i = 0; i < variable->value_count; i++)
  379   1          {
  380   2              value_copy[i] = variable->value[i];
  381   2          }
  382   1          EA = ea_backup;
  383   1      
  384   1          service_packet_append_string(packet_reply, &index, variable->name);
  385   1          service_packet_append_char(packet_reply, &index, ',');
  386   1          for(i = 0; i < variable->value_count; i++)
  387   1          {
  388   2              if(0U != i)
  389   2              {
  390   3                  service_packet_append_char(packet_reply, &index, ',');
  391   3              }
  392   2              service_packet_append_float3(packet_reply, &index, value_copy[i]);
  393   2          }
  394   1      
  395   1          service_packet_append_string(packet_reply, &index, "\r\n");
  396   1          service_wireless_uart_send_buffer((const uint8 *)packet_reply, index);
  397   1      }
  398          
  399          //-------------------------------------------------------------------------------------------------------
             -------------
  400          // 函数简介     处理读取指令
  401          // 参数说明     name            变量名称
  402          // 返回参数     void
  403          // 使用示例     内部调用，用户无需关心
  404          //-------------------------------------------------------------------------------------------------------
             -------------
  405          static void service_packet_handle_read(char *name)
  406          {
  407   1          service_packet_variable_t *variable;
  408   1      
  409   1          packet_read_total++;
  410   1          variable = service_packet_find_variable(name);
  411   1          if(NULL == variable)
  412   1          {
  413   2              packet_read_miss_total++;
  414   2              service_packet_save_miss_name(name);
  415   2              return;
  416   2          }
  417   1      
  418   1          service_packet_print_variable(variable);
  419   1      }
  420          
  421          //-------------------------------------------------------------------------------------------------------
             -------------
  422          // 函数简介     处理写入指令
  423          // 参数说明     name            变量名称
  424          // 参数说明     value_text      待写入的数值字符串
  425          // 返回参数     void
  426          // 使用示例     内部调用，用户无需关心
  427          //-------------------------------------------------------------------------------------------------------
             -------------
  428          static void service_packet_handle_write(char *name, char *value_text)
  429          {
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 8   

  430   1          uint8 i;
  431   1          uint8 changed;
  432   1          uint8 ea_backup;
  433   1          float value_copy[SERVICE_PACKET_VALUE_MAX];
  434   1          service_packet_variable_t *variable;
  435   1      
  436   1          packet_write_total++;
  437   1          variable = service_packet_find_variable(name);
  438   1          if((NULL == variable) || (NULL == value_text))
  439   1          {
  440   2              packet_write_miss_total++;
  441   2              service_packet_save_miss_name(name);
  442   2              return;
  443   2          }
  444   1      
  445   1          if(0U == service_packet_parse_float_list(value_text, value_copy, variable->value_count))
  446   1          {
  447   2              packet_write_parse_fail_total++;
  448   2              return;
  449   2          }
  450   1      
  451   1          changed = service_packet_value_changed(variable, value_copy);
  452   1          /* float 在 C251 上不是原子写；防止实时 TIM 读取到撕裂的参数帧。 */
  453   1          ea_backup = EA;
  454   1          EA = 0;
  455   1          for(i = 0; i < variable->value_count; i++)
  456   1          {
  457   2              variable->value[i] = value_copy[i];
  458   2          }
  459   1          EA = ea_backup;
  460   1      
  461   1          packet_write_ok_total++;
  462   1          if((0U != changed) && (NULL != variable->callback))
  463   1          {
  464   2              variable->callback();
  465   2          }
  466   1          service_packet_print_variable(variable);
  467   1      }
  468          
  469          //-------------------------------------------------------------------------------------------------------
             -------------
  470          // 函数简介     处理触发指令
  471          // 参数说明     name            操作名称
  472          // 返回参数     void
  473          // 使用示例     内部调用，用户无需关心
  474          // 备注信息     零延时动作在通讯服务上下文立即写入其邮箱/执行非实时逻辑；
             -延时动作才走兼容队列。
  475          //-------------------------------------------------------------------------------------------------------
             -------------
  476          static void service_packet_handle_trigger(char *name)
  477          {
  478   1          service_packet_action_t *action;
  479   1      
  480   1          action = service_packet_find_action(name);
  481   1          if(NULL == action)
  482   1          {
  483   2              return;
  484   2          }
  485   1      
  486   1          if(0U == action->delay_ms)
  487   1          {
  488   2              action->func();
  489   2          }
  490   1          else
  491   1          {
  492   2              (void)service_function_queue_add(action->func, action->delay_ms, SERVICE_PACKET_TRIGGER_PRIORITY)
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 9   

             -;
  493   2          }
  494   1      }
  495          
  496          //-------------------------------------------------------------------------------------------------------
             -------------
  497          // 函数简介     解析并处理一帧完整数据
  498          // 参数说明     frame           去除帧头帧尾后的帧内容
  499          // 返回参数     void
  500          // 使用示例     内部调用，用户无需关心
  501          // 备注信息     帧格式为 $$命令,名称,数值@@，命令支持 W/R/T
  502          //-------------------------------------------------------------------------------------------------------
             -------------
  503          static void service_packet_handle_frame(char *frame)
  504          {
  505   1          char command;
  506   1          char *name;
  507   1          char *value_text;
  508   1      
  509   1          if('\0' == frame[0])
  510   1          {
  511   2              return;
  512   2          }
  513   1      
  514   1          command = frame[0];
  515   1          name = service_packet_skip_field_separator(&frame[1]);
  516   1          value_text = name;
  517   1      
  518   1          while(('\0' != *value_text) && (0U == service_packet_is_field_separator(*value_text)))
  519   1          {
  520   2              value_text++;
  521   2          }
  522   1      
  523   1          if('\0' != *value_text)
  524   1          {
  525   2              *value_text = '\0';
  526   2              value_text++;
  527   2              value_text = service_packet_skip_field_separator(value_text);
  528   2          }
  529   1          else
  530   1          {
  531   2              value_text = NULL;
  532   2          }
  533   1      
  534   1          if('\0' == *name)
  535   1          {
  536   2              return;
  537   2          }
  538   1      
  539   1          if('R' == command)
  540   1          {
  541   2              service_packet_handle_read(name);
  542   2          }
  543   1          else if('W' == command)
  544   1          {
  545   2              service_packet_handle_write(name, value_text);
  546   2          }
  547   1          else if('T' == command)
  548   1          {
  549   2              service_packet_handle_trigger(name);
  550   2          }
  551   1      }
  552          
  553          //-------------------------------------------------------------------------------------------------------
             -------------
  554          // 函数简介     复位接收状态机
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 10  

  555          // 参数说明     void
  556          // 返回参数     void
  557          // 使用示例     内部调用，用户无需关心
  558          //-------------------------------------------------------------------------------------------------------
             -------------
  559          static void service_packet_reset_rx(void)
  560          {
  561   1          packet_rx_state = SERVICE_PACKET_RX_WAIT_HEAD_1;
  562   1          packet_frame_index = 0U;
  563   1      }
  564          
  565          //-------------------------------------------------------------------------------------------------------
             -------------
  566          // 函数简介     向帧缓存写入一个字符
  567          // 参数说明     value           待写入字符
  568          // 返回参数     void
  569          // 使用示例     内部调用，用户无需关心
  570          // 备注信息     缓存溢出时自动复位接收状态机
  571          //-------------------------------------------------------------------------------------------------------
             -------------
  572          static void service_packet_push_body_char(char value)
  573          {
  574   1          if(packet_frame_index < (SERVICE_PACKET_FRAME_MAX - 1U))
  575   1          {
  576   2              packet_frame[packet_frame_index] = value;
  577   2              packet_frame_index++;
  578   2          }
  579   1          else
  580   1          {
  581   2              service_packet_reset_rx();
  582   2          }
  583   1      }
  584          
  585          //-------------------------------------------------------------------------------------------------------
             -------------
  586          // 函数简介     输入一个接收字符并推进帧状态机
  587          // 参数说明     value           接收到的字符
  588          // 返回参数     void
  589          // 使用示例     内部调用，用户无需关心
  590          // 备注信息     检测到完整 $$...@@ 帧后自动解析执行
  591          //-------------------------------------------------------------------------------------------------------
             -------------
  592          static void service_packet_input_char(char value)
  593          {
  594   1          if(SERVICE_PACKET_RX_WAIT_HEAD_1 == packet_rx_state)
  595   1          {
  596   2              if('$' == value)
  597   2              {
  598   3                  packet_rx_state = SERVICE_PACKET_RX_WAIT_HEAD_2;
  599   3              }
  600   2              return;
  601   2          }
  602   1      
  603   1          if(SERVICE_PACKET_RX_WAIT_HEAD_2 == packet_rx_state)
  604   1          {
  605   2              if('$' == value)
  606   2              {
  607   3                  packet_frame_index = 0U;
  608   3                  packet_rx_state = SERVICE_PACKET_RX_BODY;
  609   3              }
  610   2              else
  611   2              {
  612   3                  packet_rx_state = SERVICE_PACKET_RX_WAIT_HEAD_1;
  613   3              }
  614   2              return;
  615   2          }
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 11  

  616   1      
  617   1          if(SERVICE_PACKET_RX_BODY == packet_rx_state)
  618   1          {
  619   2              if('@' == value)
  620   2              {
  621   3                  packet_frame[packet_frame_index] = '\0';
  622   3                  packet_frame_total++;
  623   3                  service_packet_handle_frame(packet_frame);
  624   3                  service_packet_reset_rx();
  625   3              }
  626   2              else
  627   2              {
  628   3                  service_packet_push_body_char(value);
  629   3              }
  630   2              return;
  631   2          }
  632   1      
  633   1          if(SERVICE_PACKET_RX_TAIL_2 == packet_rx_state)
  634   1          {
  635   2              if('@' == value)
  636   2              {
  637   3                  packet_frame[packet_frame_index] = '\0';
  638   3                  packet_frame_total++;
  639   3                  service_packet_handle_frame(packet_frame);
  640   3                  service_packet_reset_rx();
  641   3              }
  642   2              else
  643   2              {
  644   3                  service_packet_push_body_char('@');
  645   3                  service_packet_push_body_char(value);
  646   3                  packet_rx_state = SERVICE_PACKET_RX_BODY;
  647   3              }
  648   2          }
  649   1      }
  650          
  651          //-------------------------------------------------------------------------------------------------------
             -------------
  652          // 函数简介     通讯包服务初始化
  653          // 参数说明     void
  654          // 返回参数     void
  655          // 使用示例     service_packet_init();
  656          // 备注信息     初始化后需要通过 service_packet_add_variable/action 注册变量和动作
  657          //-------------------------------------------------------------------------------------------------------
             -------------
  658          void service_packet_init(void)
  659          {
  660   1          uint8 i;
  661   1      
  662   1          packet_variable_count = 0U;
  663   1          packet_action_count = 0U;
  664   1          packet_rx_byte_total = 0U;
  665   1          packet_frame_total = 0U;
  666   1          packet_read_total = 0U;
  667   1          packet_read_miss_total = 0U;
  668   1          packet_write_total = 0U;
  669   1          packet_write_ok_total = 0U;
  670   1          packet_write_miss_total = 0U;
  671   1          packet_write_parse_fail_total = 0U;
  672   1          packet_last_miss_name[0] = '\0';
  673   1          service_packet_reset_rx();
  674   1      
  675   1          for(i = 0; i < SERVICE_PACKET_VARIABLE_MAX; i++)
  676   1          {
  677   2              packet_variables[i].name = NULL;
  678   2              packet_variables[i].value = NULL;
  679   2              packet_variables[i].value_count = 0U;
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 12  

  680   2              packet_variables[i].callback = (service_packet_write_callback_t)0;
  681   2          }
  682   1      
  683   1          for(i = 0; i < SERVICE_PACKET_ACTION_MAX; i++)
  684   1          {
  685   2              packet_actions[i].name = NULL;
  686   2              packet_actions[i].func = (service_packet_action_func_t)0;
  687   2              packet_actions[i].delay_ms = 0U;
  688   2          }
  689   1          #if __DBGFLAG__
  690   1          printf(">>[service_packet_init]\r\n");
  691   1          #endif
  692   1      }
  693          
  694          //-------------------------------------------------------------------------------------------------------
             -------------
  695          // 函数简介     通讯包服务调试接口
  696          // 参数说明     void
  697          // 返回参数     void
  698          // 使用示例     service_packet_debug();
  699          //-------------------------------------------------------------------------------------------------------
             -------------
  700          void service_packet_debug(void)
  701          {
  702   1          printf("[packet:byte=%ld,frame=%ld]\r\n", packet_rx_byte_total, packet_frame_total);
  703   1          printf("[packet:R=%ld/%ld]\r\n", packet_read_total, packet_read_miss_total);
  704   1          printf("[packet:W=%ld/%ld]\r\n", packet_write_total, packet_write_ok_total);
  705   1          printf("[packet:miss=%ld,parse=%ld]\r\n", packet_write_miss_total, packet_write_parse_fail_total);
  706   1          printf("[packet:var=%ld,state=%ld]\r\n", (uint32)packet_variable_count, (uint32)packet_rx_state);
  707   1          printf("[packet:index=%ld,last=%s]\r\n", (uint32)packet_frame_index, packet_last_miss_name);
  708   1      }
  709          
  710          //-------------------------------------------------------------------------------------------------------
             -------------
  711          // 函数简介     注册可读写变量
  712          // 参数说明     name            变量名称
  713          // 参数说明     value           变量数据地址
  714          // 参数说明     value_count     变量包含的 float 数量
  715          // 返回参数     uint8           1：注册成功 0：注册失败
  716          // 使用示例     service_packet_add_variable("speed", speed_value, 2);
  717          // 备注信息     相同名称重复注册时会更新变量地址和数量
  718          //-------------------------------------------------------------------------------------------------------
             -------------
  719          uint8 service_packet_add_variable(const char *name, float *value, uint8 value_count)
  720          {
  721   1          return service_packet_add_variable_with_callback(name, value, value_count, (service_packet_write_call
             -back_t)0);
  722   1      }
  723          
  724          uint8 service_packet_add_variable_with_callback(const char *name, float *value, uint8 value_count,
  725                  service_packet_write_callback_t callback)
  726          {
  727   1          service_packet_variable_t *variable;
  728   1      
  729   1          if((NULL == name) || (NULL == value) || (0U == value_count) ||
  730   1                  (SERVICE_PACKET_VALUE_MAX < value_count))
  731   1          {
  732   2              return 0U;
  733   2          }
  734   1      
  735   1          variable = service_packet_find_variable(name);
  736   1          if(NULL != variable)
  737   1          {
  738   2              variable->value = value;
  739   2              variable->value_count = value_count;
  740   2              variable->callback = callback;
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 13  

  741   2              return 1U;
  742   2          }
  743   1      
  744   1          if(SERVICE_PACKET_VARIABLE_MAX <= packet_variable_count)
  745   1          {
  746   2              return 0U;
  747   2          }
  748   1      
  749   1          packet_variables[packet_variable_count].name = name;
  750   1          packet_variables[packet_variable_count].value = value;
  751   1          packet_variables[packet_variable_count].value_count = value_count;
  752   1          packet_variables[packet_variable_count].callback = callback;
  753   1          packet_variable_count++;
  754   1      
  755   1          return 1U;
  756   1      }
  757          
  758          //-------------------------------------------------------------------------------------------------------
             -------------
  759          // 函数简介     注册触发动作
  760          // 参数说明     name            操作名称
  761          // 参数说明     func            触发动作函数
  762          // 参数说明     delay_ms        触发后延迟执行时间 单位 ms
  763          // 返回参数     uint8           1：注册成功 0：注册失败
  764          // 使用示例     service_packet_add_action("reset", service_motor_reset, 0);
  765          // 备注信息     相同名称重复注册时会更新函数和延迟时间
  766          //-------------------------------------------------------------------------------------------------------
             -------------
  767          uint8 service_packet_add_action(const char *name, service_packet_action_func_t func, uint32 delay_ms)
  768          {
  769   1          service_packet_action_t *action;
  770   1      
  771   1          if((NULL == name) || (NULL == func))
  772   1          {
  773   2              return 0U;
  774   2          }
  775   1      
  776   1          action = service_packet_find_action(name);
  777   1          if(NULL != action)
  778   1          {
  779   2              action->func = func;
  780   2              action->delay_ms = delay_ms;
  781   2              return 1U;
  782   2          }
  783   1      
  784   1          if(SERVICE_PACKET_ACTION_MAX <= packet_action_count)
  785   1          {
  786   2              return 0U;
  787   2          }
  788   1      
  789   1          packet_actions[packet_action_count].name = name;
  790   1          packet_actions[packet_action_count].func = func;
  791   1          packet_actions[packet_action_count].delay_ms = delay_ms;
  792   1          packet_action_count++;
  793   1      
  794   1          return 1U;
  795   1      }
  796          
  797          //-------------------------------------------------------------------------------------------------------
             -------------
  798          // 函数简介     通讯包服务周期更新
  799          // 参数说明     void
  800          // 返回参数     void
  801          // 使用示例     service_packet_update();
  802          // 备注信息     从无线串口读取数据并送入帧状态机，建议在主循环中周期调用
  803          //-------------------------------------------------------------------------------------------------------
C251 COMPILER V5.60.0,  service_packet                                                     26/07/26  18:30:55  PAGE 14  

             -------------
  804          void service_packet_update(void)
  805          {
  806   1          uint8 i;
  807   1          uint8 chunk_count;
  808   1          uint8 read_buffer[SERVICE_PACKET_RX_READ_SIZE];
  809   1          uint8 read_length;
  810   1      
  811   1          for(chunk_count = 0U; chunk_count < SERVICE_PACKET_MAX_CHUNKS_PER_UPDATE; chunk_count++)
  812   1          {
  813   2              read_length = (uint8)service_wireless_uart_read_buffer(read_buffer, SERVICE_PACKET_RX_READ_SIZE);
  814   2              packet_rx_byte_total += read_length;
  815   2      
  816   2              for(i = 0; i < read_length; i++)
  817   2              {
  818   3                  service_packet_input_char((char)read_buffer[i]);
  819   3              }
  820   2      
  821   2              if(SERVICE_PACKET_RX_READ_SIZE != read_length)
  822   2              {
  823   3                  break;
  824   3              }
  825   2          }
  826   1      }


Module Information          Static   Overlayable
------------------------------------------------
  code size            =    ------     ------
  ecode size           =      5786     ------
  data size            =    ------     ------
  idata size           =    ------     ------
  pdata size           =    ------     ------
  xdata size           =      1306     ------
  xdata-const size     =    ------     ------
  edata size           =    ------     ------
  bit size             =    ------     ------
  ebit size            =    ------     ------
  bitaddressable size  =    ------     ------
  ebitaddressable size =    ------     ------
  far data size        =    ------     ------
  huge data size       =    ------     ------
  const size           =    ------     ------
  hconst size          =       286     ------
End of Module Information.


C251 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
